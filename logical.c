#include <stdio.h>

int main()
{
  int x = 10;
  int y = 20;

  // 1 - true
  // 0 - false
  printf("x == y? %d\n", (x == y));
  printf("x > y? %d\n", (x > y));
  printf("x < y? %d\n", (x < y));

  return 0;
}